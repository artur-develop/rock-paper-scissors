export enum Shapes {
  rock = "rock",
  paper = "paper",
  scissors = "scissors",
}

export enum EmitType {
  CHOOSE = "choose",
  GET_PLAYERS = "get_players",
}

export enum GameResult {
  DRAW = "no one",
}
